# ── Frontend (Vercel) ─────────────────────────────────
# The full URL to the backend API (including /api).
# For local dev:
VITE_API_URL=http://localhost:8000/api

# For production (Render backend), set this in Vercel dashboard:
#   VITE_API_URL=https://catalyst-m.onrender.com/api
